import { Text, View } from "react-native";

export default function Configuracoes() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Tela de Configurações (Desenvolvida pela Rebeca)</Text>
    </View>
  );
}