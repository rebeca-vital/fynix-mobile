import { Text, View } from "react-native";

export default function Perfil() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Tela de Perfil (Desenvolvida pela Rebeca)</Text>
    </View>
  );
}